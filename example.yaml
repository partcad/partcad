local_server:
  true # use a local server instead of a url to get credentials
  # this is necessary when using this script from localhost

videos:
  - title: PartCAD - Last 7 Days
    file: gource_output.mp4
    description: Development during last week
    category: Science & Technology
    privacy: private
    tags:
      - partcad
      - git
      - gource

secrets_path: ./client_secrets.json
credentials_path: /tmp/secrets/credentials.json
