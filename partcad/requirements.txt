# No CAD library is a hard runtime dependency of the core any more. PartCAD
# builds, renders and exports every shape in sandboxed runtimes (which install
# their own copy - see sandbox_versions), and carries geometry between them as
# BREP-byte envelopes rather than live OCP objects. cadquery-ocp, build123d and
# ocpsvg are the optional 'cad' extra (see requirements-cad.txt), needed only by
# the opt-in in-process features: convert("build123d"/"cadquery"), show() and
# 'pc import assembly'.
# sdf-fork  # installed on demand into the sandbox runtime (see part_factory_sdf.py)
pyyaml>=6.0.1
pygit2>=1.15.0
ruamel.yaml>=0.18.5
packaging>=23.1
setuptools
jinja2
requests
aiofiles
aiohttp
vyper-config==1.2.1
sentry-sdk[opentelemetry]==2.66.0
async-lru==2.3.0
filelock==3.32.0
docker>=7.1.0
jsonschema>=4.23.0
psutil>=6.1.1
