cadquery-ocp==7.9.3.1.1
# 'build123d' and 'ocpsvg' are no longer hard runtime dependencies: the core
# imports neither at import time, builds/exports shapes in sandboxed runtimes,
# and only needs build123d for the opt-in convert("build123d") live-object API.
# They are available as the 'cad' optional extra (see requirements-cad.txt).
# 'ocpsvg' is unused by the core entirely (sandboxes install it themselves).
# sdf-fork  # installed on demand into the sandbox runtime (see part_factory_sdf.py)
pyyaml>=6.0.1
pygit2>=1.15.0
ruamel.yaml>=0.18.5
packaging>=23.1
setuptools
jinja2
requests
aiofiles
aiohttp
vyper-config==1.2.1
sentry-sdk[opentelemetry]==2.66.0
async-lru==2.3.0
filelock==3.32.0
docker>=7.1.0
jsonschema>=4.23.0
psutil>=6.1.1
