__pycache__
.cache_ggshield
.conda
.coverage*
.ocp_vscode
.op/
.partcad
.pre-commit
.pytest_cache
.tox
.venv
.vs/
*.egg-info
*.prof
*.pyc
**/.DS_Store
/build
/dist
# snapcraft's working directories, and the .snap it writes into the project
# root before `dev-tools/snap/build.sh` moves it into `dist/snap/`
/parts
/stage
/prime
/*.snap
# The compatibility shim, built beside the wheel. Its LICENSE.txt is copied in
# from the repository root by the build, the way each distribution's used to be.
/dev-tools/shim/build
/dev-tools/shim/dist
/dev-tools/shim/LICENSE.txt
allure-report/
allure-results/
behave-results/
docs/build
# Compiled gettext catalogs — Sphinx auto-builds these from the .po files at build time
docs/source/locales/**/*.mo
docs/mkdocs/site/
output
pc-test-results/
pytest-results/
test-results/
*.code-workspace
# Materialized (symlink-free) plugin artifact produced by ai-agents/scripts/materialize.sh
/ai-agents/.build
# Fetched by 'fileFrom: url' in examples/produce_part_kicad -- the package
# deliberately does not carry it (see its 'software:' section), and an untracked
# copy left behind by 'pc install' would trip the rendered-examples check.
/examples/produce_part_kicad/arduino-cli-install.sh
