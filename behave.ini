[behave]
# @ai scenarios are excluded for now: the AI provider integrations are
# temporarily broken. TODO: drop "~@ai" once AI features work again.
tags = ~@wip
    ~@ai
logging_level = DEBUG
log_capture = true
paths = features/
format=allure_behave.formatter:AllureFormatter
outfiles=allure-results
