desc: This example demonstrates how to define a sketch using a CadQuery script.

docs:
  usage: |
    ```shell
    pc inspect -s sketch
    ```

sketches:
  sketch:
    type: cadquery
    desc: A sample sketch
    parameters:
      width: 3.0
      length: 4.0

render:
  readme:
  svg:
