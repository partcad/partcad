#!/bin/sh
#
# The host-side tool the device ships with. It is here as the contents of a
# 'software' object, not as anything PartCAD runs: PartCAD carries the file and
# says which one it is, and what to do with it is the reader's business.
#
echo "PartCAD example service tool"
